{ config, pkgs, ... }:

{
programs.niri.enable = true;

environment.systemPackages = with pkgs; [
   fuzzel
   alacritty
   bibata-cursors
   brightnessctl
  ];

services.input-remapper.enable = true;
}
